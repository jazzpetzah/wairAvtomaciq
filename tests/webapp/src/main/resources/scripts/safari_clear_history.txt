set menuItemToSelect to "all history"set defaultTimeout to 0.2tell application "System Events"	activate application "Safari"	delay defaultTimeout	tell process "Safari"		delay defaultTimeout		tell menu bar item "History" of menu bar 1			click			delay defaultTimeout			click menu item "Clear History and Website Data…" of menu 1			delay defaultTimeout		end tell		tell sheet 1 of window 1			tell pop up button 1				if value of it is not equal to menuItemToSelect then					click					delay defaultTimeout					tell menu 1						click menu item menuItemToSelect						delay defaultTimeout					end tell				end if			end tell			click button "Clear History"			delay defaultTimeout		end tell	end tell	#keystroke "w" using command downend tell