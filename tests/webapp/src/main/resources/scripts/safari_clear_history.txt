set defaultTimeout to 1.0tell application "System Events"	repeat until exists process "Safari"		delay defaultTimeout	end repeat	delay defaultTimeout	tell process "Safari"		repeat until exists window "Privacy"			set frontmost to true			click (menu item 1 where its name starts with "Preferences") of menu 1 of menu bar item "Safari" of menu bar 1			tell window 1				click button "Privacy" of toolbar 1			end tell			delay defaultTimeout		end repeat		tell window 1			click button "Remove All Website Data…" of group 1 of group 1		end tell		tell window 1			click button "Remove Now" of sheet 1			keystroke "w" using command down		end tell	end tellend tell