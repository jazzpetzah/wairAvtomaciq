akka {
  log-dead-letters = 0 //May be useful to increase for debugging
  log-dead-letters-during-shutdown = off
  loglevel = "INFO"
  actor {
    provider = "akka.remote.RemoteActorRefProvider"
  }
}